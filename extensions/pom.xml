<?xml version="1.0" encoding="UTF-8"?>
<!--
 Licensed to the Apache Software Foundation (ASF) under one or more
 contributor license agreements.  See the NOTICE file distributed with
 this work for additional information regarding copyright ownership.
 The ASF licenses this file to You under the Apache License, Version 2.0
 (the "License"); you may not use this file except in compliance with
 the License.  You may obtain a copy of the License at

     http://www.apache.org/licenses/LICENSE-2.0

 Unless required by applicable law or agreed to in writing, software
 distributed under the License is distributed on an "AS IS" BASIS,
 WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 See the License for the specific language governing permissions and
 limitations under the License.
-->
<project xmlns="http://maven.apache.org/POM/4.0.0" 
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" 
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">

  <modelVersion>4.0.0</modelVersion>
  <parent>
    <groupId>org.bigtranslate</groupId>
    <artifactId>bigtranslate</artifactId>
    <version>0.1</version>
    <relativePath>../pom.xml</relativePath>
  </parent>
  <artifactId>bigtranslate-extensions</artifactId>
  <packaging>jar</packaging>
  <name>Extensions</name>
  <description>Library of classes that are reused across subcomponents.</description>

  <build>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-assembly-plugin</artifactId>
        <version>${maven.assembly.plugin.version}</version>
        <configuration>
          <descriptors>
            <descriptor>src/main/assembly/assembly.xml</descriptor>
          </descriptors>
        </configuration>
        <executions>
          <execution>
            <id>dist-assembly</id>
            <phase>package</phase>
            <goals>
              <goal>single</goal>
            </goals>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>

  <dependencies>
    <dependency>
      <groupId>ai.mattmann.mnemosyne</groupId>
      <artifactId>cas-metadata</artifactId>
      <version>${oodt.version}</version>
    </dependency>
    <dependency>
      <groupId>ai.mattmann.mnemosyne</groupId>
      <artifactId>cas-filemgr</artifactId>
      <version>${oodt.version}</version>
      <exclusions>
        <!-- HSQLDB 1.8 under its pre-move coordinates, which cas-filemgr and
             cas-workflow both still declare. Maven cannot mediate it against
             org.hsqldb:hsqldb because the groupId differs, so both jars land
             on the test classpath and 1.8's org.hsqldb.DatabaseURL shadows
             2.7.4's. The 2.7.4 JDBCDriver then calls a parseURL overload the
             shadowing class does not have:

               NoSuchMethodError: org.hsqldb.persist.HsqlProperties
               org.hsqldb.DatabaseURL.parseURL(String, boolean, boolean)

             Invisible locally, where a locally built Mnemosyne does not drag
             the old coordinates in. It appears only against the published
             poms, which is what CI resolves. -->
        <exclusion>
          <groupId>hsqldb</groupId>
          <artifactId>hsqldb</artifactId>
        </exclusion>
      </exclusions>
    </dependency>
    <dependency>
      <groupId>ai.mattmann.mnemosyne</groupId>
      <artifactId>pcs-core</artifactId>
      <version>${oodt.version}</version>
    </dependency>
    <dependency>
      <groupId>ai.mattmann.mnemosyne</groupId>
      <artifactId>cas-workflow</artifactId>
      <version>${oodt.version}</version>
      <exclusions>
        <!-- HSQLDB 1.8 under its pre-move coordinates, which cas-filemgr and
             cas-workflow both still declare. Maven cannot mediate it against
             org.hsqldb:hsqldb because the groupId differs, so both jars land
             on the test classpath and 1.8's org.hsqldb.DatabaseURL shadows
             2.7.4's. The 2.7.4 JDBCDriver then calls a parseURL overload the
             shadowing class does not have:

               NoSuchMethodError: org.hsqldb.persist.HsqlProperties
               org.hsqldb.DatabaseURL.parseURL(String, boolean, boolean)

             Invisible locally, where a locally built Mnemosyne does not drag
             the old coordinates in. It appears only against the published
             poms, which is what CI resolves. -->
        <exclusion>
          <groupId>hsqldb</groupId>
          <artifactId>hsqldb</artifactId>
        </exclusion>
      </exclusions>
    </dependency>
    <dependency>
      <groupId>org.hsqldb</groupId>
      <artifactId>hsqldb</artifactId>
      <version>2.7.4</version>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>junit</groupId>
      <artifactId>junit</artifactId>
      <version>${junit.version}</version>
      <scope>test</scope>
    </dependency>
  </dependencies>

</project>
