# The pipeline's own scripts -- bt-extract-strings, bt-translate-chunk,
# bt-join-index -- import nothing outside the standard library, and the
# translation is reached over HTTP. So there is nothing to install here.
#
# ETLLib used to be listed: tsvtojson, repackage and poster were W1, which
# turned every row of every TSV into JSON and posted it. W2 deduplicates
# globally first and joins the answers back, and none of those three are
# called any more.
#
# Pantogloss is not here either, and never was: it is not published, so
# bigtranslate-setup takes it as PANTOGLOSS_SOURCE -- a checkout, a wheel, or
# any pip specifier.
