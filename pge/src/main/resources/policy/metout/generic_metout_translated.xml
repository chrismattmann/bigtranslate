<?xml version="1.0" encoding="UTF-8"?>

<metadataList>
    
   <metadata key="TagAccumulator" val="value"/>
   <metadata key="JobId" val="[JobId]"/>
   <!-- Filename stays the on-disk name so ingest can find the file. InputFiles
        is that same job JSON name, which is the EmploymentJob parent.
        ProductName is distinct so pedigree skip-self does not hide the parent. -->
   <metadata key="InputFiles" val="[Filename]"/>
   <metadata key="ProductionDateTime"/>
   <metadata key="ProductType" val="EmploymentJobTranslated"/>
   <metadata key="FileLocation"/>
   <metadata key="FileSize"/>
   <metadata key="ProductName" val="[Filename].translated.json"/>
        
</metadataList>
